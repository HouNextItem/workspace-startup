interface:
  display_name: "Workspace Startup"
  short_description: "Discover or initialize Workspace rules before projects."
  default_prompt: "Use $workspace-startup to initialize or load my Workspace before starting this project."
policy:
  allow_implicit_invocation: true