interface:
  display_name: "Workspace Startup"
  short_description: "Discover or initialize Workspace rules before projects."
  default_prompt: "Use $workspace-startup to find or initialize my Workspace before starting this project. If a Workspace is created, tell me the folder name."
policy:
  allow_implicit_invocation: true
